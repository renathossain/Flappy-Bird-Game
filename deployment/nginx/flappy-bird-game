server {
    listen 80;
    server_name _; # Use _ to match any hostname (suitable for IP access)

    # Serve static files from the frontend build directory
    location / {
        root /home/username/project-overreact-native/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
}
